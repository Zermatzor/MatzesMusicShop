@model MatzesMusicShop.ViewModels.LayoutViewModel
<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Matzes CD Shop", "Index", "Home", null, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav ">
                <li>@Html.ActionLink("CD's", "Index", "CDs")</li>
                <li>@Html.ActionLink("Warenkorb" + "(" + Model.CartItemsCount.ToString() + ") " + Model.Price + "€", "Index", "ShoppingCart")</li>
            </ul>
        </div>
    </div>
</div>